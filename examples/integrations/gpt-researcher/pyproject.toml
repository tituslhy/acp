[project]
name = "acp-gpt-researcher"
version = "0.1.0"
requires-python = ">=3.11,<4.0"
dependencies = [
    "acp-sdk",
    "duckduckgo-search>=7.0.0",
    "gpt-researcher>=0.12.19",
]

[tool.uv.sources]
acp-sdk = { workspace = true }
